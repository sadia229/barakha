import 'package:flutter/material.dart';

class TrackFastingScreen extends StatelessWidget {
  const TrackFastingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Track Fasting Screen"),
      ),
    );
  }
}
