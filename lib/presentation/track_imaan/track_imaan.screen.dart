import 'package:flutter/material.dart';

class TrackImaanScreen extends StatelessWidget {
  const TrackImaanScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Track Imaan Screen"),
      ),
    );
  }
}
