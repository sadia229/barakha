import 'package:flutter/material.dart';

class FastingDietScreen extends StatelessWidget {
  const FastingDietScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Fasting Diet Screen"),
      ),
    );
  }
}
